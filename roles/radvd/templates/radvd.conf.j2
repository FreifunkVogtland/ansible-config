interface bat0 {
  AdvSendAdvert on;
  IgnoreIfMissing on;

  AdvManagedFlag off;
  AdvOtherConfigFlag on;
  AdvLinkMTU 1280;

  MaxRtrAdvInterval 30;

  prefix {{ ROUTERRANGEV6 }}/64 {
    AdvOnLink on;
    AdvAutonomous on;
    AdvRouterAddr on;
  };

  RDNSS {{ ROUTERIPV6 }} {
  };
};
