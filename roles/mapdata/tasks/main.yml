---
- name: install mapdata dependencies
  apt: pkg={{ item }} state=installed
  with_items:
  - apache2

- name: add apache2 mapdata site
  copy:
    src: apache2-mapdata.conf
    dest: /etc/apache2/sites-enabled/apache2-mapdata.conf
  notify:
  - restart apache2
